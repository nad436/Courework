#ifndef SHIP_H
#define SHIP_H
// Ship class represents a type of ship with its size and count
class Ship {
public:
    int size;
    int count;

    Ship(int s, int c);
};

#endif // SHIP_H
